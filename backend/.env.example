# Environment
NODE_ENV=development

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=techfolks_db
DB_SSL=false
DB_LOGGING=false

# Database Pool Configuration
DB_MAX_CONNECTIONS=100
DB_MIN_CONNECTIONS=20
DB_IDLE_TIMEOUT=60000
DB_CONNECTION_TIMEOUT=10000
DB_STATEMENT_TIMEOUT=30000
DB_DESTROY_TIMEOUT=5000
DB_REAP_INTERVAL=1000
DB_CREATE_RETRY_INTERVAL=200

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_CONNECT_TIMEOUT=10000
REDIS_COMMAND_TIMEOUT=5000
REDIS_MAX_CONNECTIONS=50
REDIS_KEEP_ALIVE=30000

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS Configuration
FRONTEND_URL=http://localhost:5173,http://localhost:3000,http://localhost:3002

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
EMAIL_FROM=noreply@techfolks.com
EMAIL_FROM_NAME=TechFolks

# Judge0 Configuration
JUDGE0_URL=http://localhost:2358
JUDGE0_AUTH_TOKEN=
JUDGE0_DEFAULT_CPU_TIME_LIMIT=2.0
JUDGE0_DEFAULT_MEMORY_LIMIT=256
JUDGE0_MAX_POLL_ATTEMPTS=30
JUDGE0_POLL_INTERVAL=1000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_AUTH_MAX=50
RATE_LIMIT_SUBMISSION_MAX=10
RATE_LIMIT_SUBMISSION_WINDOW_MS=60000

# Cache Configuration
CACHE_PREFIX=techfolks
CACHE_DEFAULT_TTL=3600
CACHE_SHORT_TTL=300
CACHE_MEDIUM_TTL=1800
CACHE_LONG_TTL=3600
CACHE_VERY_LONG_TTL=86400
CACHE_WEEK_TTL=604800

# Test User Configuration (for seeding)
TEST_USER_USERNAME=testuser
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=testuser123
TEST_USER_FULLNAME=Test User
TEST_USER_RATING=1200
TEST_USER_MAX_RATING=1200

# Bcrypt Configuration
BCRYPT_SALT_ROUNDS=10

# Logging
LOG_LEVEL=info
LOG_DIR=logs

# API Configuration
API_TIMEOUT=10000

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# AWS S3 Configuration (Optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=

# RabbitMQ Configuration
RABBITMQ_URL=amqp://localhost

# MongoDB Configuration (for logs/analytics)
MONGODB_URI=mongodb://localhost:27017/techfolks_logs

# Session Configuration
SESSION_SECRET=your-session-secret-change-this-in-production
SESSION_TIMEOUT=86400000

# Sentry Configuration (Optional)
SENTRY_DSN=

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth (Optional)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=